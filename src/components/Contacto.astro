<section id="contacto" class="bg-white text-black py-16">
    <div class="container mx-auto text-center">
        <h2 class="text-3xl font-bold mb-6">Contáctanos</h2>
        <p class="mb-4">Si tienes alguna duda, no dudes en escribirnos.</p>
        <form action="#" method="POST" class="max-w-lg mx-auto">
            <input
                type="text"
                placeholder="Tu nombre"
                class="w-full p-4 mb-4 border border-gray-300 rounded"
                required
            />
            <input
                type="email"
                placeholder="Tu correo"
                class="w-full p-4 mb-4 border border-gray-300 rounded"
                required
            />
            <textarea
                placeholder="Tu mensaje"
                class="w-full p-4 mb-4 border border-gray-300 rounded"
                required></textarea>
            <button
                type="submit"
                class="bg-black text-white py-2 px-6 rounded-lg">Enviar</button
            >
        </form>
    </div>
</section>
